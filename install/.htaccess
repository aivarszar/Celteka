# Allow direct access to install directory
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow PHP execution
<FilesMatch "\.php$">
    SetHandler application/x-httpd-php
</FilesMatch>
